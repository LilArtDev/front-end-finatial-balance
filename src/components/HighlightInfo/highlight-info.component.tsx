import React from "react";

export interface HighlightInfoProps {
  label: string;
  value: number;
  currency: string;
}

export function HighlightInfo({ currency, label, value }: HighlightInfoProps) {
  return (
    <div>
      <div></div>
    </div>
  );
}
